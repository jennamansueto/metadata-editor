import React from 'react';
import PlaceholderPage from './PlaceholderPage';

const MetadataTypesPage: React.FC = () => (
  <PlaceholderPage title="Metadata Types" description="Metadata type management will be available in a future migration phase." />
);

export default MetadataTypesPage;
