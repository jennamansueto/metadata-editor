import React from 'react';
import PlaceholderPage from './PlaceholderPage';

const ExternalResourceImportPage: React.FC = () => (
  <PlaceholderPage title="Import External Resources" description="External resource import will be available in a future migration phase." />
);

export default ExternalResourceImportPage;
